version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: monthly
    ignore:
      # Pinned to the exact version Home Assistant core ships.
      - dependency-name: PyTurboJPEG
